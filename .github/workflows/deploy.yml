name: deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-trade
  cancel-in-progress: false

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    permissions:
      contents: read

    env:
      AWS_REGION: us-west-2
      S3_BUCKET: trade.yeyaxin.com
      CLOUDFRONT_DISTRIBUTION_ID: E34RSZK4DII7XD

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Write .env.local for Next.js
        env:
          NEXT_PUBLIC_FINNHUB_API_KEY: ${{ secrets.NEXT_PUBLIC_FINNHUB_API_KEY }}
        run: |
          if [ -n "$NEXT_PUBLIC_FINNHUB_API_KEY" ]; then
            printf 'NEXT_PUBLIC_FINNHUB_API_KEY=%s\n' "$NEXT_PUBLIC_FINNHUB_API_KEY" > .env.local
            echo "wrote .env.local with finnhub key (length=${#NEXT_PUBLIC_FINNHUB_API_KEY})"
          else
            echo "WARNING: NEXT_PUBLIC_FINNHUB_API_KEY secret is empty; ticker search will be limited to fallback list"
          fi

      - name: Build (static export)
        env:
          NEXT_PUBLIC_FINNHUB_API_KEY: ${{ secrets.NEXT_PUBLIC_FINNHUB_API_KEY }}
        run: npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Sync HTML + assets to S3 (short cache)
        run: |
          # The agent-runner publishes its own files to runs/*.json and
          # portfolios/*-synthesis.json. Exclude those from the --delete sweep
          # so a site rebuild doesn't blow away agent-produced content.
          # NOTE: do NOT exclude *.txt — Next.js emits __next._tree.txt
          # RSC payloads alongside HTML for client-side Link navigation.
          aws s3 sync out/ s3://${{ env.S3_BUCKET }}/ \
            --exclude "_next/static/*" \
            --exclude "runs/*.json" \
            --exclude "portfolios/*-synthesis.json" \
            --cache-control "public, max-age=300, s-maxage=86400" \
            --delete

      - name: Sync _next/static to S3 (immutable cache)
        run: |
          aws s3 sync out/_next/static/ s3://${{ env.S3_BUCKET }}/_next/static/ \
            --cache-control "public, max-age=31536000, immutable"

      - name: Invalidate CloudFront
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ env.CLOUDFRONT_DISTRIBUTION_ID }} \
            --paths "/trade" "/trade/*" "/trade/_next/*"
